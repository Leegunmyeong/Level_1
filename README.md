<!-- 2026/06/01 -->
# 대제목

## 중제목

### 소제목

* 목록내용

1. 순서내용

-------------------------------------------------------------

# 파일명 규칙

* `파일명 html`
* 파일명은 공백, 한글, 특수문자 금지 #자주 쓰는 단축어

# 자주 쓰는 단축어

* `html:5` 쓰고 Tab 키 누르기

* `Ctrl + /` 한줄 주석
* ` Shift + Alt + A` 선택영역 주석
* `Shift + Alt + 상하방향키` = 복제

-------------------------------------------------------------
# HTML 생성 후 구조태그 작성 순서
1. `html:5` 쓰고 Tab 자동완성
2. `lang="en"` 을 `"ko"`로 변경하기
3. `title` 제목 작성하기
4. `meta` 작성하고 자동목록 중 `kw`선택하고 키워드 작성하기
    <meta name="keywords" content="교보, 문고, 교보문고"><!-- 검색 키워드(개수제한없음) -->
5. `meta` 작성하고 자동목록 중 `desc` 선택하고 요약 작성하기
    <meta name="description" content="교보문고 회원이 아니신가요? 가입하면 다양한 책 할인혜택이 있습니다."><!-- 사이트 검색 시 나타나는 요약설명 -->
<!-- (위) 각 디바이스(모바일, 태블릿, pc)의 너비에 자동으로 맞추기 -->
6. `body` 내부에 웹의 주요 내용 작성할 준비하기
-------------------------------------------------------------

tags01.html, tags2.html <!-- 2026/06/02 -->

<!-- 개념정리 -->
블록(코끼리) or 인라인(뱀)
제목태그 H1~6 + 블록

Class
<!-- 사용 목적 -->
* 공통 디자인 or 동일 유형 요소들 그룹화 할때 사용

<!-- Class 특징 -->
* 반복 사용가능
* 한번에 2개 이상 이름 적용가능

Id
<!-- 사용목적 -->
* 특별한 요소 지정할 때 사용

<!-- Id 특징 -->
* 반복 사용 X

<!-- 태그(특징 및 용도)-->

<!-- 블록 -->
* `<h1>h1~h6</h1>` -> 제목(대중소)
* `<p></p>` -> 내용 작성 or 설명
* `<hr>` -> 구분선 생성
* `<blockquote></blockquote>` -> 긴 인용문

<!-- 인라인 -->
* `<em></em>`-> 중요 부분 글꼴 형태 변환
* `<br>`-> 내용 중바꿈
* `<strong></strong>` -> 중요키워드 강조
* `<q></q>` -> 짧은 인용
* `<del></del>` -> 교체 or 삭제된 내용 표시
* `<s></s>` -> 유효하지않는 내용 
* `<code></code>` 프로그래밍 코드 구분용
* `<sub></sub>`, `<sup></sup>` -> 윗첨자&아래첨자
* `span`:  인라인 그룹, 디자인이 다른 요소를 묶기

<!-- 특수문자태그 -->
* `&`: 특수문자태그의 시작기호
* `;`: 특수문자태그의 종료기호
* `&lt;, &gt;`: 왼쪽꺽쇠(<), 오른쪽꺽쇠(>)
* `&copy;`: © copyright 저작권 기호

-------------------------------------------------------------
# git 
* git clone 깃주소붙여넣기
-------------------------------------------------------------
# 다양한 문서 태그-2
## block tag
* `div`: 2개 이상 자식을 묶는 그룹, class 또는 id 필수
* `ul-li: ul(비순차 그룹), li(목록) 엄격한 부모-자식관계
* `ol-li: ol(순차 그룹), li(목록) 엄격한 부모-자식관계
* `dl-dt-dd`: dl(정의형그룹), dt(정의형제목), dd(정의내용)
    * 검색키워드로 사용하지 않는 소제목과 내용 `h-p`를
    `dt-dd`로 대체 가능
* `a`: 블록의 부모로 사용 시 링크(div등 그룹 대체 가능)
## inline tag
* `span`:  인라인 그룹, 디자인이 다른 요소를 묶기
* `a`: 인라인의 형제로 사용 시 링크
### 비슷한 의미의 대체 태그들
* `h1~h6` vs `dt`
    * 중요도가 높고 검색키워드로서 사용되야 한다면 h1~6태그
    * 제목~내용이 바로 연결되고 중요도가 낮으며 키워드로서
    사용가치가 낮을 때 dt태그
* `p` vs `li` vs `dd` vs `span`
    * 제목 하단에 단순 단락으로 구성된 내용 표시 p태그
    * 2개 이상 연속된 목록 구성 시 li태그
    * 중요도와 키워드가 낮은 제목 dt옆 내용을 감쌀땐 dd
    * 인라인으로 추가로 감싸야하는 내용이 있을 때 span
* `div` vs `ul` vs `ol` vs `a` vs `dl` vs `span`
    * div - 구역을 분리하고 서로 다른 값을 줘야하는 요소 같은 행에 여러개 있을 때(내 생각)
            2개 이상의 블록 or 인라인 자식으로 있을 때 그룹(선생님 생각)
    * ul - 순서가 중요하지않는 list 형식(내 생각)
           순서가 없는 목록이 2개 이상있을때 그룹(선생님 생각)
    * ol - 요소의 순서가 중요한 list 형식(내 생각)
           순서가 있는 목록이 2개 이상있을때 그룹(선생님 생각)
    * a - 요소 전체가 버튼처럼 클릭되서 똑같은 링크경로로 가질 때(내 생각)
          링크로 선택되는 태그가 2개 이상 있을 때 그룹(선생님 생각)
    * dl - 중요도가 낮은 제목-내용(dt-dd)가 있을 때 그룹
    * span - 2개 이상의 인라인을 묶어야 할때 그룹



 



